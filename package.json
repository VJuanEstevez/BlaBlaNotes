{
  "name": "blablanotes",
  "version": "1.0.0",
  "private": true,
  "description": "Captura, clasifica y guarda ideas y listas por voz usando la Web Speech API.",
  "scripts": {
    "build:css": "sass scss/main.scss css/main.css --no-source-map --style=expanded",
    "watch:css": "sass scss/main.scss css/main.css --no-source-map --style=expanded --watch",
    "serve": "python3 -m http.server 5173"
  },
  "devDependencies": {
    "sass": "^1.77.8"
  }
}
